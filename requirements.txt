# Version lower-bounds are anchored to a known-working local dev environment
# snapshot (see docs/reproducibility.md). They are NOT verified against the
# actual L4/A100 training VMs used for reported experiments in this repo --
# treat them as a floor, not a guarantee, until re-verified on that hardware.

# Core
numpy>=2.4.2
pillow>=12.1.1
pyyaml>=6.0  # dataset manifest parsing (tools/validate_dataset.py)

# Training
torch>=2.10.0
torchvision>=0.25.0
transformers>=5.2.0
datasets>=4.8.3
huggingface_hub>=1.5.0

# Plotting / analysis
pandas>=3.0.1
matplotlib>=3.10.8
